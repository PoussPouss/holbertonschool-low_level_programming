#include "main.h"

/**
*_abs - checks lowercase character
*@n: is the character to return 1 if lowercase
*Return: 1 if c is lowercase, 0 otherwise
*/
int _abs(int n)

{
	if (n >= 0)

	return (n);

	else

	return (-n);

}

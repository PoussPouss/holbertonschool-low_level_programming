#include "main.h"

/**
*_isupper - checks uppercase character
*@c: is the character to return 1 if upper
*Return: 1 if c is lowercase, 0 otherwise
*/
int _isupper(int c)
{

	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}

